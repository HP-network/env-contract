# Security Policy

Do not open a public issue for a credential or a reproducible security problem involving a live service. Email `hpnetwork@hpnetwork.top` with the affected version, reproduction steps, and a minimal sanitized example.

Never include API keys or complete request headers in reports. `env-contract` is a local validator and does not transmit values to a provider.
